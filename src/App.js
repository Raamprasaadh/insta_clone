import './App.css';

function App() {
  return (
    <div className="App">
      {/**header */}
      {/** 
       * body
       * left component
       * **status bar - slider
       * **posts infinite scrolling
       * right component
       * **userprofile
       * **suggestions for you bar
       * **footer
      */}
      {/** */}
    </div>
  );
}

export default App;
